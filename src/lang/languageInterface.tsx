export interface Menu {
  hello: string;
  Iam: string;
  name: string;
  about: string;
  works: string;
  contact: string;
}

export interface AboutContent {
  header: string;
  about: string;
  content: string[];
}
